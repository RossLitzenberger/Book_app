<%= form_for ([@book, @book.comments.build]) do |f| %>
  <%= form_errors_for @book%>
  <p>
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </p>
  <br>
  <p>
    <%= f.submit class: "btn btn-success" %>
  </p>
<% end %>
